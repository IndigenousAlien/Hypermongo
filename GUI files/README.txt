This folder holds the .ui and .py files which I used to create the GUI windows for HyperMongo.

The .UI files were created using PyQt5's designer program, which were then converted to python files using the terminal command "pyuic5 name.py name.ui"
